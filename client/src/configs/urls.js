const baseURL = 'http://localhost:5000/'

const urls = {
    products: 'api/products',
    categories: '/api/categories',
    types: '/api/types',
    users: '/api/users',

    account: '/account'

}

export {
    baseURL, urls
}